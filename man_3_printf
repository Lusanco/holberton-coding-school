.TH MAN_3_PRINTF 3 "November 2023" "Version 1.0" "Man Printf Manual"

.SH NAME
man_3_printf \- formatted output conversion

.SH SYNOPSIS
.B #include <stdio.h>
.B int man_3_printf(const char *format, ...);

.SH DESCRIPTION
The \fBman_3_printf\fR function writes output to the standard output (stdout) under the control of a \fIformat\fR string.

The format string is composed of zero or more directives: ordinary characters (not %), which are copied unchanged to the output stream, and conversion specifications, each of which results in fetching zero or more arguments.

.SH RETURN VALUE
Upon successful completion, \fBman_3_printf\fR returns the number of characters printed (excluding the null byte used to end output to strings), or a negative value if an error occurs.

.SH EXAMPLES
.B #include <stdio.h>
.B int main() {
.B     int n = man_3_printf("Hello, %s!\n", "world");
.B     printf("Characters printed: %d\n", n);
.B     return 0;
.B }

This will output:
.B
Hello, world!
Characters printed: 13

.SH SEE ALSO
.BR printf (3),
.BR fprintf (3),
.BR sprintf (3),
.BR snprintf (3),
.BR vprintf (3).

.SH AUTHOR
Your name and contact information.

.SH BUGS
No known bugs.

.SH COPYRIGHT
Copyright information.
