#!/usr/bin/env python3

"""Calculates the floor (largest integer
less than or equal to) of a float.

Args:
    n (float): The number to find the floor of.

Returns:
    int: The floor of n.
"""

import math


def floor(n: float) -> int:
    """Returns the floor of a float."""
    return math.floor(n)
