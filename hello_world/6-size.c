#include <stdio.h>

/**
 * main - Prints size of
 * various types on the
 * computer it is compiled
 * and run on.
 * Return: 0
 */

int main(void)

{
	/*Variables*/
	int myInt;
	float myFloat;
	long myLong;
	long long int myLolong;
	char myChar;

	printf(myChar);
	printf(myInt);
	printf(myLong);
	printf(myLolong);
	printf(myFloat);

	return (0);
}
